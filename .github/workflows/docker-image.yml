name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  buildx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      #- uses: docker/setup-buildx-action@v2
       # id: buildx
       # with:
       #   install: true
          
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: isanjay112/courses:0.0.1
        
      # - name: Build
      #  run: |
      #    docker build . -f Courses.API/Dockerfile -t isanjay112/courses:0.0.$GITHUB_RUN_NUMBER # will run buildx
      #- name: Inspect builder
      #  run: |
      #    echo "Name:      ${{ steps.buildx.outputs.name }}"
      #    echo "Endpoint:  ${{ steps.buildx.outputs.endpoint }}"
      #    echo "Status:    ${{ steps.buildx.outputs.status }}"
      #    echo "Flags:     ${{ steps.buildx.outputs.flags }}"
      #    echo "Platforms: ${{ steps.buildx.outputs.platforms }}"
          
      
      # - name: Push
      #  run: |
      #    docker image push isanjay112/courses:0.0.$GITHUB_RUN_NUMBER
          

#  build:
#
#    runs-on: ubuntu-latest
#    env:
#      DOCKER_REPOSITORY: isanjay112 # name of Docker Hub ID
#      IMAGE_NAME: courses
#      IMAGE_TAG: ${{ github.run_number }} # $GITHUB_RUN_NUMBER
#
#  # Never amend commit again.
#    steps:
#    - uses: actions/checkout@v3
#    - name: Build the Docker image
#      run: docker build ./Courses.API --file ./Courses.API/Dockerfile --tag $DOCKER_REPOSITORY/$IMAGE_NAME:$GITHUB_RUN_NUMBER


